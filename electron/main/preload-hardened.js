'use strict';

// Custom preload wrapper.
// This file is NOT generated by expo-electron and is safe to edit.
// It loads the generated preload (ElectronNative bridge) and then hardens
// the exposed IPC surface to be fail-fast.

// eslint-disable-next-line global-require
require('./preload.js');

// eslint-disable-next-line global-require
require('./preload-hardening.js');
